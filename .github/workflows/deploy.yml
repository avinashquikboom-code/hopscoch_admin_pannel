name: Build & Push Admin Panel

# Build the image and push it to AWS ECR on every push to main.
# Deployment to the server is MANUAL (pull + ./deploy-admin.sh on the VPS).
on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  # Hardcoded ECR target (no secrets needed for region/repo).
  AWS_REGION: ap-south-1
  IMAGE_URI: 546941058055.dkr.ecr.ap-south-1.amazonaws.com/hopscotch-admin

jobs:
  build-and-push:
    name: Build image and push to ECR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to ECR
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # NEXT_PUBLIC_API_URL is inlined into the client bundle at build time,
          # so it must be a build arg — setting it in admin.env at container start
          # would be too late. No /api suffix: the admin appends /api to its own
          # request paths and uses this as the image origin.
          docker build \
            --build-arg NEXT_PUBLIC_API_URL=https://api.fciseller.com \
            -t "$IMAGE_URI:$IMAGE_TAG" \
            -t "$IMAGE_URI:latest" \
            .
          docker push "$IMAGE_URI:$IMAGE_TAG"
          docker push "$IMAGE_URI:latest"
          echo "Pushed $IMAGE_URI:latest (and :$IMAGE_TAG)"
